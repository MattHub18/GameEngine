package com.company.ai.decisions;

import java.util.Vector;

public class UtilityBaseSystem {
    public static int getPositionByPoints(Vector<Task> subtasks) {
        if (subtasks.size() == 1)
            return 0;
        return 0;
    }
}
